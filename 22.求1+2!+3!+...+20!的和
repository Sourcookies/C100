#include <stdio.h>
int main() {
	double sum = 0.0;
	for (int i = 1; i <= 20; i++)
	{
		double tmp = 1;
		for (int j = 1; j <= i; j++)
		{
			tmp *= j;
		}
		sum += tmp;
	}
	printf("%lf\n",sum);
	
	      return 0;
	}

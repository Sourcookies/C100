#include <stdio.h>
int main() {
	int a, n;
	scanf_s("%d %d", &a, &n);
	int s = 0;
	int tmp = a;
	for (int i = 0; i < n; i++)
	{
		s += tmp;
		tmp = tmp * 10 + a;
	}
	printf("a+aa+...=%d\n", s);
	
	      return 0;
	}
